-- RETIRED 2026-09-20, with the legacy site it was written for.
--
-- It credited `points` in znote_accounts -- the old shop's currency, on a site
-- that never had a shop, so nothing could ever be spent. Those tables are going.
--
-- It never fired here anyway: this server's own premium scroll registers the
-- same item first (data/scripts/itemevents/use/others/premium_scroll.lua) and
-- grants the 30 days of premium time item 16101 actually describes.
--
-- Kept, disabled by the leading '#', so the next person to wonder what the
-- legacy scroll did can read it rather than guess.
--
-- local function doPlayerAddPremiumPoints(cid, count)
--     db.query('UPDATE znote_accounts SET points = points+'.. count ..' WHERE account_id = ' .. getAccountNumberByPlayerName(getCreatureName(cid)) .. ' LIMIT 1')
-- end
--
-- local function onUse(cid, item, fromPosition, itemEx, toPosition) 
--     doPlayerAddPremiumPoints(cid, 100)
--     doPlayerSendTextMessage(cid, MESSAGE_EVENT_ADVANCE, "You have recived 100 Shop Credits to your balance.")
--     doSendMagicEffect(getCreaturePosition(cid), 28)
--     doRemoveItem(item.uid,1)
--     return true
-- end
--
-- -- registrations generated from the pack XML by harness/build_itemevents.py
-- local realmapEvent1 = ItemEvent()
-- realmapEvent1:type("use")
-- realmapEvent1.onUse = onUse
-- realmapEvent1:id(16101)
-- realmapEvent1:register()
